# Artifact Hub repository metadata (ADR-0708), pushed to
# ghcr.io/<owner>/kollect:artifacthub.io by the release workflow.
#
# 2026-08-18 — repository registered on Artifact Hub as `kollect`
# (https://artifacthub.io/packages/search?repo=kollect). repositoryID below is the
# real ID from the Artifact Hub control panel.
#
# Verified Publisher activates once BOTH hold: the pushed repositoryID matches the
# registered one, AND the owner email below matches the email on the Artifact Hub
# account that owns the repository (account `konih`). Artifact Hub does not send a
# verification mail — it compares the two addresses directly.
repositoryID: cb3be9a6-8e3b-4419-9de5-1184fe349c29
owners:
  - name: Konrad Heimel
    email: konrad.heimel@gmail.com
