# Dependabot version updates — action SHA bumps and Go module patches.
# Dependabot alerts + security updates are enabled at the repo level (GitHub Settings →
# Code security); this file configures scheduled version-update PRs only.
# https://docs.github.com/en/code-security/dependabot
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    groups:
      github-actions:
        patterns:
          - "*"
    commit-message:
      prefix: ":arrow_up:"
      include: scope

  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 10
    groups:
      go-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
    commit-message:
      prefix: ":arrow_up:"
      include: scope
