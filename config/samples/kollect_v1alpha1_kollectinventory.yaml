apiVersion: kollect.dev/v1alpha1
kind: KollectInventory
metadata:
  name: team-inventory
  namespace: default
  labels:
    app.kubernetes.io/name: kollect
    app.kubernetes.io/managed-by: kustomize
spec:
  # Primary MVP path: Postgres (or Kafka). Git sink is for audit/CI — see git-inventory-demo.
  sinkRefs:
    - postgres-inventory-demo
  suspend: false
