# T002/T003 — K-05: scope-deny must unregister the namespaced target

- Feature: `specs/001-namespaced-scope-enforcement/` · branch: `fm/kollect-b3-scope` · base: `3919327` · started: 2026-09-24
- Satisfies: US1 / FR-1 · plan: Design §K-05 · constitution: unfilled template — none
- Smallest coherent change: call `Engine.UnregisterTarget` in the `enforceTarget` deny branch before degrading, mirroring the cluster path.
- Files in scope: `internal/controller/kollecttarget_controller.go`, `internal/controller/kollecttarget_scope_unregister_test.go` (+ review-driven additions below)
- Status: REVIEW
- Next action: independent review; then tick tasks.

## Verification matrix

| # | Claim / invariant | Sensor | Class | Result |
|---|---|---|---|---|
| 1 | batch lock "register→deny→no items": after a deny reconcile the engine holds no registration and store is empty | `TestScopeDenyUnregistersTarget` | machine (test written in-lane) | pass |
| 2 | late informer objects after the deny are not stored (frozen set cannot keep flowing) | `TestScopeDenyStopsLaterItems` | machine | pass |
| 3 | deny still persists `Degraded/scopeReasonNSDenied` (status behaviour unchanged) | `TestScopeDenyUnregistersTarget` (deg assertion) | machine | pass |
| 4 | transient `ScopeLookupFailed` deny re-registers via requeue (no indefinite halt) — review finding | `TestScopeLookupFailureHaltsAndRequeues` | machine | pass |
| 5 | policy denies (GVK/NS) return nil error (no hot requeue loop) | `TestScopeDenyUnregistersTarget` returns nil err; pre-existing `cluster_scope_enforce_envtest_test.go` enforceTarget specs | machine | pass |
| 6 | unregister-before-degrade ordering matches cluster analogue | code reading of `cluster_scope_enforce.go:85,96` | agent-review | pass |

## Sensors run

| Tier | Command | Exit | Seconds | Note |
|---|---|---|---|---|
| inner | `go test ./internal/controller -run 'TestScopeDeny\|TestScopeLookup\|TestKollectTargetReconciler_mapScopeToTargets' -count=1` | 0 | 69 (wall, incl. build) | green |
| task | `go test ./internal/controller/... -count=1` (envtest assets) | 0 | 40 | full package incl. ginkgo suite |
| milestone | `make test` (all pkgs, coverprofile) | 0 | ~10m | sink/git env flake triaged below |

## Iterations

1 · pre-fix red run · IMPLEMENTATION · `ItemCount after deny = 1` / `store = 2 after late object` → added unregister to deny branch.
2 · `make lint` · IMPLEMENTATION · gocyclo 21 on Reconcile after adding the nil-engine guard → extracted `degradeScopeDenied` helper.
3 · adversarial review · IMPLEMENTATION · lookup-fail deny had no requeue path → `scopeReasonLookupFailed` error return + `TestScopeLookupFailureHaltsAndRequeues`.

## Harness gaps

- `TestScopeDenyStopsLaterItems` observes absence for a fixed 300 ms window; a hypothetical delivery slower than the window would hide a regression. The synchronous unregister (row 1) is the primary sensor.

## Maintainability

1. Locality — yes: deny branch + its test file only (+ const in `scope_check.go`, shared by inventory path).
2. Change surface — ~15 product lines + helper for one behaviour; ratio fine.
3. Duplication — reuses existing `UnregisterTarget`, mirrors cluster `unregisterAll` pattern, no copy-paste of logic.
4. Second way — none; same unregister call the suspend/delete branches use.
5. Coupling — controller already held the engine; no new knowledge.
6. Abstraction — uses `scopeCheck` result + `setDegraded` as before.
7. Debt for green — `degradeScopeDenied` extraction was lint-driven but improves legibility; not a special case.
8. Legibility — helper + comments name the K-05 rationale; clearer than the inline version.
Easier: tightening the deny path for future scope reasons. Harder: none observed.

## Independent review

Round 1 (fresh subagent, diff-only, mutation-verified): **APPROVE**.

- [P2] lookup-retry error not recorded in `retErr` (metrics classified the requeue as success) — FIXED in `ee6f602`.
- [P2] `record.NewFakeRecorder(10)` blocks if >10 events (brittleness, not wrong at ≤3 events) — accepted.
- Dispute on row 4: recovery-after-blip re-registration is inferred, not exercised — accepted as agent-review reasoning; phase-1 re-registration proves the same path.
- Late-object mutation confirmed the delivery path is real (store=2 red on unregister removal).

## Verdict

CLOSED — rows 1–5 machine (row 6 agent-review), review APPROVE, P2 fixed. Status: CLOSED.
