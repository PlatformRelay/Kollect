# T011–T013 — gates + adversarial self-review

- Feature: `specs/001-namespaced-scope-enforcement/` · branch: `fm/kollect-b3-scope` · base: `3919327` · started: 2026-09-24
- Satisfies: batch gate (repo tests green) + loop step 3 (adversarial review) · constitution: unfilled template — none
- Status: REVIEW
- Next action: fold reviewer returns from T002–T010 files; then T014 (push + PR).

## Verification matrix

| # | Claim / invariant | Sensor | Class | Result |
|---|---|---|---|---|
| 1 | `make test` (all non-e2e packages, envtest) green | `make test` | machine | pass (exit 0, cover total 78.3%) |
| 2 | golangci-lint v2 clean | `make lint` → `0 issues.` | machine | pass |
| 3 | package-graph gate clean | `task arch-lint` → "OK - No warnings found" | machine | pass |
| 4 | markdown gate over tracked docs incl. `specs/` | markdownlint-cli2 (repo config) → 0 issues in 152 files | machine | pass |
| 5 | `gofmt`/`go vet` clean | `make test` fmt/vet stages; `gofmt -l internal/ cmd/` empty | machine | pass |
| 6 | adversarial review produced and fixed its finding | review report below + commit `991040a` + `TestScopeLookupFailureHaltsAndRequeues` | machine (fix locked) | pass |

## Sensors run

| Tier | Command | Exit | Seconds | Note |
|---|---|---|---|---|
| task | `make test` | 0 | ~10m | cover.out 78.3% (internal floor 65) |
| task | `make lint` | 0 | ~4m | one re-run after fixing 3 findings (gocyclo, 2 shadow) |
| task | `task arch-lint` | 0 | ~1m | |
| task | markdownlint (docs/node_modules, repo config) | 0 | ~1m | |
| inner | `go build ./... && go vet ./internal/...` | 0 | ~1m | |

## Iterations

1 · `make lint` · IMPLEMENTATION · gocyclo(21) on `Reconcile` + govet shadow of suite-level `ctx` in new tests → helper extraction + `rctx` rename.
2 · full-suite second pass · ENVIRONMENT · `internal/sink/git` `TestExportRemote_PushesFiles` failed on the developer machine's global gpg commit-signing (pinentry OOM under tmux): proven environment-only — same test green with `GIT_CONFIG_GLOBAL=/dev/null`. No product code touched.
3 · full-suite second pass · ENVIRONMENT · `TestEngine_ScaleEnvtest500` timed out only under whole-tree parallel load; green standalone (`-run` alone, exit 0). Not classified as regression.

## Harness gaps

- The two flakes above are host/load-sensitivity, not coverage gaps; CI (clean git config, dedicated runners) is the authoritative re-run.

## Maintainability

1. Locality — gate task touches no product code (review-driven fixes live in T002/T003 evidence).
2–7. n/a except q7: none — no gate was weakened.
8. Legibility — flakes recorded rather than suppressed.
Easier: future lanes see the env-flake triage pattern. Harder: nothing.

## Adversarial self-review (loop step 3, skill: adversarial-review)

Verdict: CONCERNS (1 WARNING — fixed) then CLEAN.
- WARNING (Saboteur): transient `ScopeLookupFailed` now unregisters but returned nil error → collection halted with no requeue until an unrelated event. Fixed: error return + const + lock test (commit `991040a`).
- NOTE (New Hire): `namespaceMatches` repeats the `metadata.name` pin check in the enforced branch — deliberate, different rationale per branch.
- NOTE (Security Auditor): K-09 admission tightening blocks updates of pre-existing off-allowlist inventories (parity with cluster webhook); called out in the PR body.
- NOTE (Budget Holder): `.specify/` scaffolding deliberately left untracked (commit `a6372ff`) to avoid cross-lane conflicts; `specs/` deliverables tracked.
- Could not check: no kind/e2e run locally (CI territory); docker-less host for sink containers.

## Independent review

Reviewer returns are recorded in the per-finding evidence files (T002-T003, T004-T006, T007-T008, T009-T010).

## Verdict

pending reviewer returns.
