# T007/T008 — K-08: KollectScope watch on the namespaced target controller

- Feature: `specs/001-namespaced-scope-enforcement/` · branch: `fm/kollect-b3-scope` · base: `3919327` · started: 2026-09-24
- Satisfies: US3 / FR-3 · plan: Design §K-08 · constitution: unfilled template — none
- Smallest coherent change: `Watches(&KollectScope{}, mapScopeToTargets)` enqueueing all targets in the scope's namespace.
- Files in scope: `internal/controller/kollecttarget_controller.go`, `internal/controller/kollecttarget_map_test.go`
- Status: REVIEW
- Next action: independent review; then tick tasks.

## Verification matrix

| # | Claim / invariant | Sensor | Class | Result |
|---|---|---|---|---|
| 1 | scope write enqueues exactly the targets in the scope's namespace, none elsewhere | `TestKollectTargetReconciler_mapScopeToTargets` | machine | pass |
| 2 | foreign object type ⇒ nil (no spurious reconciles) | same test | machine | pass |
| 3 | every (not just enforcing) scope is mapped on purpose — lowest-name rule | mapper comment + reviewer judgement of rationale parity with `mapClusterScopeToClusterTargets` | independent-review | pending |
| 4 | RBAC supports the watch (kollectscopes get;list;watch) | pre-existing marker `kollecttarget_controller.go:38` + `make test`'s `manifests` regeneration with zero diff | machine (git-clean after regenerate) | pass |
| 5 | delete events also trigger the mapper | controller-runtime `Watches` default handler behaviour | agent-review | pass |

## Sensors run

| Tier | Command | Exit | Seconds | Note |
|---|---|---|---|---|
| inner | `go test ./internal/controller -run 'TestKollectTargetReconciler_map' -count=1` | 0 | 69 (wall, shared build) | green |
| task | `go test ./internal/controller/... -count=1` | 0 | 40 | full package |
| task | `make test` regenerate step (`controller-gen` manifests/RBAC) | 0 | — | no manifest drift, RBAC already sufficient |

## Iterations

None; TDD order (mapper test red on compile, green after implementation).

## Harness gaps

- Row 5 (delete-event delivery) is library semantics, not tested in-lane; an envtest could register the controller and assert the reconcile, but the existing profile-watch mapper shares the identical untested assumption.

## Maintainability

1. Locality — yes, both named files.
2. Change surface — ~30 lines for one watch.
3. Duplication — mapper mirrors `mapProfileToTargets` structure (repo pattern), no shared helper extracted.
4. Second way — none.
5. Coupling — controller learns KollectScope exists for *mapping* only; enforcement already lived in `scopeCheck`.
6. Abstraction — standard controller-runtime watch.
7. Debt for green — none.
8. Legibility — rationale comment states why no filtering.
Easier: scope changes act within one event, not 60 s. Harder: nothing notable.

## Independent review

Round 1 (fresh subagent, mutation-verified): **APPROVE**.

- [P2] test was namespace-only; duplicate-key loophole — FIXED: test now asserts exact name set {deploys, pods} each once.
- [P2] no watch predicate ⇒ every scope write enqueues N namespace targets — kept: parity with profile/cluster mappers, workqueue dedupes, N is per-namespace-small.
- Row 4 re-verified by reviewer independently (marker + `config/rbac/role.yaml:96-105`).

## Verdict

CLOSED — rows 1, 2, 4 machine; row 5 agent-review (library semantics, recorded gap); review APPROVE, P2 fixed. Status: CLOSED.
