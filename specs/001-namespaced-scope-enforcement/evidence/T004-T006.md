# T004–T006 — K-06: empty effective namespace set must fail closed under a ceiling

- Feature: `specs/001-namespaced-scope-enforcement/` · branch: `fm/kollect-b3-scope` · base: `3919327` · started: 2026-09-24
- Satisfies: US2 / FR-2, FR-5 · plan: Design §K-06 · constitution: unfilled template — none
- Smallest coherent change: carry a `scopeEnforced` flag on `targetState` (derived from a non-empty `ScopeCeiling`) and honour it in `namespaceMatches`; re-pin the contract test that had locked the old fallback as "intended".
- Files in scope: `internal/collect/engine.go`, `internal/collect/collection_filter.go`, `internal/collect/engine_helpers_test.go`, `internal/collect/engine_scope_ceiling_test.go`
- Status: REVIEW
- Next action: independent review; then tick tasks.

## Verification matrix

| # | Claim / invariant | Sensor | Class | Result |
|---|---|---|---|---|
| 1 | batch lock "empty-effective fail closed" at unit level: enforced + empty set ⇒ no namespace matches except `metadata.name` pin | `TestEngineNamespaceMatches` | machine (test rewritten in-lane to the new contract) | pass |
| 2 | batch lock at engine level: ceiling emptied the effective set ⇒ already-collected items evicted, store stays empty | `TestEngineCeilingEmptyEffectiveFailsClosed` | machine | pass |
| 3 | FR-5: ceiling-free targets keep selector/pin fallback (no behaviour change for unscoped tenants) | `TestEngineNamespaceMatches` (non-enforced rows) + `TestEngineCeilingFreeRegistrationCollects` | machine | pass |
| 4 | full collect suite unaffected (dispatch hot path, all existing engine semantics) | `go test ./internal/collect/... -count=1` | machine | pass |
| 5 | cluster-synthetic registration never hits the enforced-empty branch (always registers with a non-empty explicit set) | reading of `kollectclustertarget_controller.go` `syncEngineTargets` | agent-review | pass |
| 6 | re-pin of the "pinned-as-intended" test is deliberate, not a weakening — register §K-06 orders exactly this | register entry (diff-dsk H2) + reviewer judgement | independent-review | pending |

## Sensors run

| Tier | Command | Exit | Seconds | Note |
|---|---|---|---|---|
| inner | `go test ./internal/collect -run 'TestEngineNamespaceMatches|TestEngineCeiling' -count=1` | 0 | 29 (wall) | green |
| task | `go test ./internal/collect/... -count=1` (envtest assets) | 0 | 25 | whole package |
| milestone | `make test` full tree | 0 | ~10m | |

## Iterations

No red→green cycles beyond TDD: the re-pinned helper test failed against the old signature/behaviour (compile + assertion), then passed after the flag was threaded.

## Harness gaps

- Row 5 is reading-only: a cluster-scope regression that registered a synthetic target with an empty set would now deny that namespace silently. Cheapest future sensor: a `RegisterTarget` table test asserting `scopeEnforced`/pin interaction for synthetic shapes.

## Maintainability

1. Locality — yes: collect package + its tests.
2. Change surface — 3 files of product interest for one flag; minimal.
3. Duplication — pin-check repeated once in the enforced branch; kept inline deliberately (different rationale per branch).
4. Second way — `CeilingRestrictsNamespaces` is the single source for "does the ceiling bound anything"; used by registration only.
5. Coupling — engine does not learn about controllers; flag derived from data the engine already receives (`opts.ScopeCeiling`).
6. Abstraction — extends the existing `RegisterTargetOptions` flow; no bypass.
7. Debt for green — none; no value hard-coded for tests.
8. Legibility — comments state the K-06 rationale at both decision points.
Easier: any future fail-closed rule has a home in `namespaceMatches`. Harder: registration options carry one more derived semantic.

## Independent review

(pending)
