# T009/T010 — K-09: namespaced inventory admission must enforce the scope sink-ref allowlist

- Feature: `specs/001-namespaced-scope-enforcement/` · branch: `fm/kollect-b3-scope` · base: `3919327` · started: 2026-09-24
- Satisfies: US4 / FR-4 · plan: Design §K-09 · constitution: unfilled template — none
- Smallest coherent change: call `scope.ValidateInventoryFamilySinkRefs` inside the existing `binding.Enforced` block of the namespaced inventory validator, returning `validation.ScopeViolationErrors`, mirroring the cluster webhook.
- Files in scope: `internal/webhook/v1alpha1/kollectinventory_webhook.go`, `internal/webhook/v1alpha1/kollectinventory_webhook_test.go`
- Status: REVIEW
- Next action: independent review; then tick tasks.

## Verification matrix

| # | Claim / invariant | Sensor | Class | Result |
|---|---|---|---|---|
| 1 | batch lock "webhook sink-ref": off-allowlist ref at admission ⇒ denied, error names the ref | `TestKollectInventoryValidator_scopeSinkRefAllowlist` (off-list row) | machine | pass |
| 2 | on-allowlist ref admitted (no over-rejection) | same test (on-list row) | machine | pass |
| 3 | non-enforced namespace admission unchanged (FR-5) | same test (team-b row) + `TestKollectInventoryValidator_ValidateCreate` | machine | pass |
| 4 | update path shares the check (ValidateUpdate re-runs validate) | `TestKollectInventoryValidator_ValidateUpdate_nonDeletionRevalidates` proves re-validation; check itself shared via `validate()` | machine + agent-review | pass |
| 5 | reconcile-path parity: same validator function/allowlist fields (`snapshotSinkRefs` et al.) | code reading `scope_check.go:87` vs webhook | agent-review | pass |
| 6 | existing interval-floor behaviour untouched | `TestKollectInventoryValidator_scopeFloorEnforced` still green in package run | machine | pass |

## Sensors run

| Tier | Command | Exit | Seconds | Note |
|---|---|---|---|---|
| inner | `go test ./internal/webhook/v1alpha1 -run 'TestKollectInventoryValidator' -count=1` | 0 | 26 (wall) | green |
| task | `go test ./internal/webhook/... -count=1` (incl. envtest admission specs) | 0 | 32 | whole webhook tree |
| milestone | `make test` full tree | 0 | ~10m | |

## Iterations

1 · first test build · TEST · wrong struct placement of `snapshotSinkRefs` (top-level `KollectScopeSpec`, not `ScopeCeilingSpec`) — corrected test against CRD source, product untouched.

## Harness gaps

- No envtest admission-layer test for the new denial (the floor case has one at webhook level only in-lane). Cheapest sensor: extend `inventory_update_admission_envtest_test.go` with an off-allowlist UPDATE.

## Maintainability

1. Locality — yes: the validator + its test.
2. Change surface — 9 product lines for admission parity.
3. Duplication — mirrors cluster webhook block; shared through `scope` + `validation` packages, not copy-pasted logic.
4. Second way — none; same helper `enforceInventory` uses.
5. Coupling — webhook already imported `scope`; no new edge.
6. Abstraction — uses the established `ScopeViolationErrors` admission wrapper.
7. Debt for green — none.
8. Legibility — block comment names the drift it closes.
Easier: admission/reconcile drift is now visible in one place. Harder: operators with pre-existing off-allowlist inventories must fix refs before further updates (admission tightened — flagged in PR).

## Independent review

Round 1 (fresh subagent, mutation-verified: removal reddens the off-list row; an over-rejection variant is caught by the on-list rows): **APPROVE**.

- [P2] only snapshot family exercised at webhook level — FIXED: database-family denied/admitted rows added to the same test (`ee6f602`); event family remains covered at the `internal/scope` unit layer (shared switch).
- [P2] `ValidateUpdate` early-return on deletionTimestamp allows a spec-changing terminating update — pre-existing, identical in the cluster webhook, reconcile degrade backstops it; noted in the PR body, out of task scope.

## Verdict

CLOSED — rows 1–3, 6 machine; row 4 machine (update re-validation) + structural; row 5 agent-review; review APPROVE, P2 family coverage fixed. Status: CLOSED.
