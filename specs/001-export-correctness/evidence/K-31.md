# K-31 — register gap (no recoverable definition)

**Verdict:** CLOSED-WITH-GAPS
**Change:** none possible in code — the finding has no definition.

## Finding

The unify report's findings register (§4, `data/kollect-unify/report.md`) contains
**no K-31 entry**. The ID appears only in the batch table (§6, line 442), which
assigns K-31 to batch B1. Register IDs K-27, K-31 and K-43 are all absent from §4
while the report's headline claims **82 findings** (79 present + 3 gaps = 82), so
K-31 was dropped during unification editing.

## Verification matrix

| Claim / invariant | Sensor | Evidence class | Result |
| --- | --- | --- | --- |
| No K-31 entry exists in the register | `grep -n 'K-31' data/kollect-unify/report.md` (batch-table hit only); `grep -oE 'K-[0-9]+' … \| sort -u` | machine | pass |
| No K-31 in any of the six source-leg reports | `grep -rln 'K-31' data/kollect-*/report.md` → no hits | machine | pass |
| The export surface both call sites of any export finding are fixed and tested | K-01/K-02 evidence (`TestExportSizeBand_*`) | machine | pass |

## Decision recorded

The batch's export fixes cover the full export surface — the sink boundary and
both the namespaced and cluster controller call sites — which is the plausible
subject matter for a fourth export-family finding. The gap is reported to the
captain rather than guessed at; the register should be corrected to either define
K-31 or remove it from the batch table.
