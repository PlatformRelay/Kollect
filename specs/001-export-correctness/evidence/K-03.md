# K-03 — "Spill" described as a mechanism that does not exist

**Verdict:** CLOSED (docs truth; captain call C-3 = (b))
**Change:** docs describe an **inline cap** with a loud `SpillRequired` skip and
state explicitly that no automatic spill write path ships.

## Verification matrix

| Claim / invariant | Sensor | Evidence class | Result |
| --- | --- | --- | --- |
| `docs/REQUIREMENTS.md` no longer claims object-store spill is mandatory/implemented | `grep -i spill docs/REQUIREMENTS.md`; read §6 | machine (grep) + agent-review | pass |
| `docs/adr/0103-etcd-limit.md` item 7 states no PVC/spill write path ships | read item 7 + diagram | agent-review | pass |
| `docs/adr/0405-export-data-contract.md` bounded-size clause reflects loud failure | read clause 4 | agent-review | pass |
| `docs/crds/index.md` describes the inline cap + per-part guidance | read section | agent-review | pass |
| Operator docs (`ANNOTATIONS-LABELS`, `troubleshooting`, `load-test-runbook`) consistent | grep + read | agent-review | pass |

## Sensors run

- `grep -n -i spill docs/**` — remaining hits are metric names
  (`kollect_export_spill_warn_total`, `spill_required`) and one accurate capability
  description in `adr/0417`; metric names are retained for dashboard stability.

## Harness gaps

No automated docs-vs-code gate covers the inline-cap wording; verification is
agent-review plus grep. Recorded as a gap (B11/B12 own docs gates).
